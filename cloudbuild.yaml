steps:
  # Listar o conteúdo do diretório de trabalho (etapa inicial)
  - name: "gcr.io/cloud-builders/gcloud"
    entrypoint: "ls"
    args: ["-la", "/workspace"]

  # Construir a imagem Docker
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/cool-healer-390700/vistoimagem:${_V1}", "."]

  # Empurrar a imagem Docker para o Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/cool-healer-390700/vistoimagem:${_V1}"]

  # Implantar no App Engine
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      - "app"
      - "deploy"
      - "--image=gcr.io/cool-healer-390700/vistoimagem:${_V1}"
      - "--version=${_V1}"
      - "--project=cool-healer-390700"
      - "--quiet"

images:
  - "gcr.io/cool-healer-390700/vistoimagem:${_V1}"
